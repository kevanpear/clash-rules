# 由 scripts/build_clash.py 从 source/geosite_claude_dns.yaml 编译，请勿手改。
# 要改规则请改 source/geosite_claude_dns.yaml，push 后 CI 会自动重编译。
# behavior: domain, format: yaml
payload:
  - '+.anthropic.com'
  - '+.claude.ai'
